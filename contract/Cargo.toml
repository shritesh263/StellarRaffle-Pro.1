[package]
name = "stellar-raffle-contract"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "22.0.2"

[dev-dependencies]
soroban-sdk = { version = "22.0.2", features = ["testutils"] }
